{
    // Use IntelliSense para saber los atributos posibles.
    // Mantenga el puntero para ver las descripciones de los existentes atributos 
    // Para más información, visite: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Iniciar programa",
            /* Ubicación del programa a Ejecutar / Depurar. */
            // Ubicación en examples.
            //"program": "${workspaceFolder}\\_code\\examples\\WitAiRequest.mjs",
            // Ubicación del index.js.
            "program": "${workspaceFolder}\\_code\\index.mjs",
            // Argumento necesario para poder usar la instrucción "import ... from "...".
            // Función aún experimental en la versión de Node.js v10.x LTS, planeada a incluir en un futuro.
            "runtimeArgs": [
                "--experimental-modules"
            ]
        }
    ]
}